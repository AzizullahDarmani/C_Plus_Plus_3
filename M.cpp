#include<bits/stdc++.h>
using namespace std;

int main() {
    int sum = 0;
    int num;
    while (cin >> num) {
        sum += num;
    }
    cout << "Sum of all elements of array is " << sum << endl;
    return 0;
}